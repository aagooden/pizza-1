<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2>Select an item</h2>
    <h3>Each additional topping costs $1</h3>
    <form action="/add_custom" method="post">
      <input type="hidden" name="selection[]" value="custom pizza">
      <input type="hidden" name="selection[]" value="12.99">
      Quantity: <input type="number" min="1" max="50" name="selection[]" value="1">
      <br>Size:
      <% menu["size"].each do |value| %>
        <input type="radio" name="selection[]" value="<%=value[0]%>"> <%=value[0].capitalize%>
      <% end %>
      <ul>
        <% menu["custom"].each do |key, value| %>
        <li><%=key.capitalize%>
          <ul>
            <% value.each do |ingred| %>
            <li> <input type="checkbox" name="ingreds[]" value="<%=ingred%>"> <%=ingred.capitalize%></li>
            <% end %>
          </ul>
        </li>
        <% end %>
      </ul>
      <input type="submit" name="" value="Add this item">
    </form>
    <a href="/menu"><button>Return to menu</button></a>
  </body>
</html>
